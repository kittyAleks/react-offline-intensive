// Файл с данными о пользователе
// Core
import { createContext } from 'react';
// Создаем экземпляр контекста для хранения данных о пользователе
export const { Provider, Consumer } = createContext(); // createContext возвр-ет два компонента Provider, Consumer

